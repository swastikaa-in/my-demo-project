#Docusign Integration Configuraiton

docusignSignatureHeader: x-docusign-signature-1
docusignSecret: uXeKjhJIdzaKnJInCWq2jKbCPvIpUuPBLOV7OMKzRCs=

#Actuator Management Endpoints Configuration

management:
  health:
    diskspace:
      enabled: false
  endpoint:
    env:
      enabled: true
    health:
      show-details: always
  endpoints:
    web:
      base-path: /
      exposure:
        include: '*'

server:
  port: 9081

